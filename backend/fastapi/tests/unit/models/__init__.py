# Unit tests for the FastAPI models/ package (Pydantic DTOs and validators).
#
# Each file maps 1-to-1 with a source module under models/.
# Tests cover schema validation, field constraints, and serialisation round-trips.
