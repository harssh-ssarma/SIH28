// Unit tests (Vitest + React Testing Library) for shared UI components.
//
// Covers: DataTable, TimetableGrid, PageHeader, ConfirmDeleteDialog, Avatar.
// No router, no SWR — all data and callbacks injected as props.
