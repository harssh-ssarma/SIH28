# Unit tests for engine/hardware/ sub-package.
#
# Each file maps 1-to-1 with a source module under engine/hardware/.
# All psutil/py-cpuinfo/cloud metadata calls are mocked to avoid
# environment-specific behaviour in CI.
