# Unit tests for Django model layer.
#
# Each file maps 1-to-1 with a source module under academics/models/.
# Tests cover model methods, properties, constraints, and signals.
# All tests use @pytest.mark.django_db and PostgreSQL test DB (never SQLite).
