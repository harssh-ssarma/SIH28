# Unit tests for core services layer (core/services/).
#
# Each file maps 1-to-1 with a source module under core/services/.
# External dependencies (Django client, Redis) are mocked so services
# are tested in pure logical isolation.
