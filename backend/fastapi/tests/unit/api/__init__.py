# Unit tests for the FastAPI api/ layer.
#
# Each file maps 1-to-1 with a router or middleware module.
# Business logic is mocked — these tests verify routing, auth guards,
# request parsing, and error serialisation only.
